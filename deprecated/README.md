# Deprecated key material

Historical certificate and key backups moved out of active protocol directories.
Nothing under this tree is referenced by runtime code or tests.

This directory is **gitignored** (except this README). Keep backups on disk locally only;
do not commit private keys or certificates.
